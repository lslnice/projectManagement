services:
  backend:
    image: ghcr.io/lslnice/projectmanagement-backend:latest
    volumes:
      - /www/wwwroot/projectManagement/data:/app/data
    environment:
      - DATABASE_PATH=/app/data/project_management.db
    restart: unless-stopped

  frontend:
    image: ghcr.io/lslnice/projectmanagement-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
